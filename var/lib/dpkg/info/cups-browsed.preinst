#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = install ] || [ "$1" = upgrade ]; then
	if [ -e "/etc/init.d/cups-browsed" ] && [ -L "/etc/init.d/cups-browsed" ] \
	   && [ $(readlink -f "/etc/init.d/cups-browsed") = /lib/init/upstart-job ]
	then
		rm -f "/etc/init.d/cups-browsed"
	fi
fi
# End automatically added section
